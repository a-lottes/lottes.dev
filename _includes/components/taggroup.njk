<ul class="cluster taggroup" data-pagefind-ignore>
  {% for tag in tags | filterTagList %}
    {% set tagUrl %}{{ (tagsUrl or "/blog/tags/") | locale_url }}{{ tag | slugify }}/{% endset %}
    <li>
      <a href="{{ tagUrl }}" class="tag">{{ tag }}</a>
    </li>
  {% endfor %}
</ul>
